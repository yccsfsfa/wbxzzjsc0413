apiVersion: v1
kind: Service
metadata:
  name: wbxzzjsc
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wbxzzjsc"
    port: 8080
  selector:
    app: wbxzzjsc
